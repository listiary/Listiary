-	Create new branch for the arc panel
Implement SCV favicons for all pages, as we have done in m.arc module
-	Git push the bug fixes after we have screwed some concatenations after implementing the SQL Schema abstraction for the m.session module
-   Add this index - `ALTER TABLE `housekeeping_itemid_filename` ADD INDEX `idx_filename` (`filename`);`
	Checked this out, but AI told me we don't need that, because of our primary key, which indexes that.
Make that {nlcomment} in a first item does not produce a new line



Features:

Bugs:



Spark v 0.1
Basic functionality rewritten from the previous Linux used CLI framework start.
But rewritten to functions and CLI frontends.
Changed the architecture, that is. 
Also, added new functions as well.

Spark v 0.2
Renamed the corefunctions and files to a better naming scheme.

// RAW FILE ACTIONS
// This is very simple - used to view, store and retrieve files from the DB
// At this point, those files we upload will not be compiled and thus not available in our wiki.
	
